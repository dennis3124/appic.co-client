export interface CompanyModel {
  name: string;
  images: Array<string>;
  description: string;
  clicks: Array<string>;
  favorites: Array<string>;
  companyUrl: string;
  ownerId: string;
  _id: string;
}
